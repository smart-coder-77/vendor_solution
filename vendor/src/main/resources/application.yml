spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vendor_db
    username: postgres
    password: server
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
